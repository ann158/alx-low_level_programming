#include <studio.h>
/**
 * main-prints the phrase "programming is like builiding a multilingual puzzle
 * Return: 0 if exited properly,non-zero otherwise
 */
int main(void)
{
	puts("\"programming is like builiding a multilingual puzzle");
	return (0);
}
